# Milestone plan — Admin-to-member email messaging

Milestone title (exact): 🔴 UNRESOLVED — the Step 5.1 ladder reached rung 4 (prompt) and the prompt could not be answered in this preview run. Rung 1: no `Milestone:` line and no inline statement. Rung 2: no `versioning` key, so treated as absent. Rung 3: no GitHub milestone titles readable (no remote/network in this preview) and no git tags (the consumer repo is not a git repository). Rung 4 outcomes, unrun: a version you type is used verbatim as the exact title; declining yields the goal-derived name `Admin-to-member email messaging` with no semver. No value was fabricated to fill this field.
Version provenance: prompted — PENDING (the rung-4 prompt is unanswered; provenance becomes `prompted` if you type a version, `declaration` if you decline)
Source brief: file:/private/var/tmp/wsvc.pbm4cQC7/repo/brief.md

## Original brief
# Brief: Admin → member messaging

Let admins send an email message to members. An admin composes a message
(a subject and a body) and sends it to a selected set of members.

In scope:
- An admin compose-and-send screen: pick the member recipients, write a subject
  and body, send.

Out of scope:
- Redesigning the member directory.
## End original brief

## Milestone description (Wave order)
Adds admin-message sender settings to the app's shared email config, so the outbound admin-message capability has a configured sender to send through. The compose-and-send screen, the send path, recipient selection, the delivery-failure log and its screen, and the send audit are not in this milestone: they are parked or dropped pending the suppression / unsubscribe (opt-out) policy decision. The member directory is not redesigned.

## Waves
- Wave 1: #A

## Issues
### #A — Configure the admin-message sender in the shared email config   [logic, risk:light]   [implied — review / trim / augment]
## Summary

Admin-to-member messaging needs a sender identity for outbound messages. This adds admin-message send settings (sender name and address) to the existing shared email config, reusing the already-configured provider rather than adding a new one. A dependent capability sends admin messages using the settings this issue defines.

## Acceptance criteria

- [ ] `src/config/email.ts` exposes admin-message sender settings (from name, from address) alongside the existing provider configuration.
- [ ] The added settings define the shape only; no working sender value is hardcoded in source.
- [ ] The addition adds no second or fallback provider configuration; admin messages and transactional email share the one already-configured provider.
- [ ] The existing transactional-email settings already in `src/config/email.ts` are unchanged by the addition.

## Non-goals

- Sending admin messages using these settings is out of scope; the dependent send capability consumes them separately.
- The literal sender value (from name, from address) is a deploy-time configuration value; this issue defines the config shape, not the value.

## Design (recorded, consistent)

- The admin-message sender settings are added to the existing `src/config/email.ts` module, not a new config file, mirroring how the existing provider settings are organized there. (`project/conventions.md#Engineering conventions`)
- The admin-message send capability reuses the already-configured email provider; this issue adds no second provider integration. (`project/conventions.md#Engineering conventions`)
- Convention followed: `project/conventions.md#Engineering conventions`

### #B — Send an admin message to the selected members through the shared EmailService   [parked — needs product input]
marker only; no fabricated body — see the needs-product-input report

### #C — Select member recipients on the admin compose screen   [parked — needs product input]
marker only; no fabricated body — see the needs-product-input report

### #D — Add the admin compose-and-send screen on AdminFormLayout   [dropped — depends on parked #B, #C]
marker only; a dependent of a parked issue cannot build, so it is not carried (Step 5 drop pass)

### #E — Record admin-message sends that exhaust their retries in EmailDeliveryLog   [dropped — depends on parked #B]
marker only; a dependent of a parked issue cannot build, so it is not carried (Step 5 drop pass)

### #F — Surface admin-message delivery failures with resend on the admin delivery-failure screen   [dropped — depends on parked #B, and on dropped #E]
marker only; a dependent of a parked issue cannot build, so it is not carried (Step 5 drop pass)

### #G — Audit every admin-message send in the shared AuditLog   [dropped — depends on parked #B]
marker only; a dependent of a parked issue cannot build, so it is not carried (Step 5 drop pass)

## Project-docs grounding
- A new outbound capability configures the already-chosen provider in the shared email config rather than picking a new one (#A) — grounded in `project/conventions.md#Engineering conventions`
- Every send goes through the shared `EmailService` and retries transient failures on the standard backoff via `sendWithRetry()` (#B) — grounded in `project/conventions.md#Engineering conventions`
- A send that exhausts its retries is recorded in the shared `EmailDeliveryLog` and surfaced on the admin delivery-failure screen with a resend action (#E, #F) — grounded in `project/conventions.md#Engineering conventions`
- Every outbound message is audited: who sent what, to whom, and when, via the shared `AuditLog` (#G) — grounded in `project/conventions.md#Engineering conventions`
- Admin compose / edit screens reuse the existing `AdminFormLayout` (#D) — grounded in `project/conventions.md#Engineering conventions`
- Empty and error states are required on every user-facing surface (#C, #D, #F) — grounded in `project/conventions.md#Engineering conventions`
- Degradations:
  - `projectDocs` resolves to `project/`, not `.project/`; it holds one doc with one section, so the grounding digest is that single slice and every citation above resolves to it.
  - The standing docs state no layering convention, so the architect's layer pass was a no-op: no candidate carries a `layer` field and ordering is dependency-only.
  - `#A`'s file-map resolved empty (it cites no `path:line` or `path (anchor)` seed, and no folder matches the keyword fallback under `sourceGlobs: ["src/**"]` — no `src/` tree exists on disk).
  - No consumer issue-template resolved (no `agentIssueTemplate` key, no `.github/ISSUE_TEMPLATE/` directory), so the issue was authored to the built-in §4 default structure.
  - The Step 5.1 version ladder could not resolve offline: rung 3 needs a GitHub milestones read and a git tag read, and neither is available here. See the identity field above.
  - `uiSurfaceGlobs` is present, so no all-candidates-are-logic degradation applies.
  - The source files the conventions doc names (`src/services/EmailService.ts`, `src/config/email.ts`, `src/models/EmailDeliveryLog.ts`, `src/models/AuditLog.ts`, `src/components/AdminFormLayout.tsx`) are stated by that doc but are not present on disk in this repo, so every citation above is to the doc rather than to a sibling `file:line`.

## Needs human input
see .milestone-feeder/needs-product-input-admin-to-member-email-messaging.md

---
This plan file is the build artifact — run `/milestone-feeder:create` to deploy it to GitHub (it ensures the labels, creates-or-adopts the milestone by the exact title above, opens each surviving issue, rewrites the slug references to real issue numbers, and patches the milestone description with the Wave order). `plan` wrote no GitHub state.

## Preview-run notes (harness-only appendix: not part of the plan-file contract)

Everything above this heading is the runner's emitted plan file, recorded byte-verbatim.
Nothing in it was edited, tidied, or corrected. `Source brief:` names a path under
`/private/var/tmp/`, not under `tests/scenarios/`, because the run was given a materialized
copy of the input set rather than the fixture directory itself. See "Containment" below.

The parked-gap report the same run emitted is the sibling file
`needs-product-input-observed-2026-08-13.md`, also byte-verbatim.

### Dispatch ledger

Three dispatches produced this output. A fourth, the grader, was made after the run had
finished and took no part in producing anything above.

| # | Dispatcher | Agent | Subject |
|---|---|---|---|
| 1 | orchestrator | `general-purpose` | the `plan` runner |
| 2 | runner | `milestone-feeder:architect` | Step 3, the once-per-run breakdown |
| 3 | runner | `milestone-feeder:issue-author` | Step 4, candidate `#A` |
| (post-run) | orchestrator | `general-purpose` | the independent grader |

Both feeder agents are the **real registered types**, resolved from the installed plugin at
version `0.13.2`, not proxies. Zero re-dispatches fired: the Step 4 verify passed on its
first return, and no return was discarded.

The table above is the orchestrator's ledger, not the runner's. It is the runner's returned
ledger of 2 rows (rows 2 and 3 here, which the runner's own notes below total as 2) plus the
two dispatches the orchestrator made itself. The stated count of 3 is rows 1 through 3: the
`(post-run)` grader row is deliberately outside it, because the grader produced none of the
output above and the count is a claim about what did.

### Containment, and why the paths above look the way they do

The permitted input set (`brief.md`, `project/`, `feeder-env.md`) was copied into a fresh
directory outside every repository, verified by SHA-256 against the fixture sources, and that
directory was handed to every dispatch as the consumer repo. It contained no answer key, no
prior observed artifact, no sibling scenario, no `tests/` directory, and no parent leading to
any of them. The plan procedure and the plugin docs it cites were materialized the same way
and verified the same way, so no dispatch was ever given a path under the plugin cache, which
carries its own full copy of the fixture tree.

The directory name carries no scenario identity, and no prompt printed the fixture's real
path. Every dispatch was required to return an itemized list of every path it read, as a
mandatory field rather than an optional confession.

### Exposures, all self-disclosed

Five exposures landed outside the permitted input set. Four stayed inside the two materialized
roots and reached nothing prohibited:

- The architect ran a `find` for a `citation-format.md` across the whole procedure root,
  matching nothing, then attempted to read that non-existent path.
- The architect attempted to list a `src/` directory in the consumer repo, which does not
  exist.
- The issue-author probed `.git`, `.github`, and `src/` in the consumer repo. None exists.
- The runner probed the consumer repo for a git repository, which it is not.

One read landed outside both roots. The architect disclosed that every Bash call initializes
a shell from the user's profile chain under `/Users/ken`, so the shell process reads that
chain on each call. The same holds for the runner's own Bash calls. It is a shell
initialization side effect rather than a deliberate read, no dispatch inspected or used any of
it, and neither process enumerated the specific files.

No dispatch reported reading any path under `/Users/ken/repos/milestone-feeder`, any
`expected.grader.md`, any prior observed artifact, or any sibling scenario. Detection is
self-report throughout, the standing convention for this harness.

### Residual exposure this run did not close

Every dispatch runs with a process working directory of `/Users/ken/repos/milestone-feeder`,
which contains `tests/` and therefore both this scenario's answer key and the control twin's.
Placing the run root outside that tree removes every reason to go there and no dispatch
reported doing so, but a working directory is not something a prompt can remove. A
`PreToolUse` deny would close it. That is hook configuration belonging to the repo owner, and
it was deliberately not written here.

### Preview emit, verbatim

The text the run printed to the user, recorded exactly as emitted. It carries the
anti-fixation prompt and the implied-candidate table.

```
Standing by while I turn the brief into a milestone + issues. This is read-only on GitHub — I'll write a reviewable plan file and create nothing on GitHub. Run `/milestone-feeder:create` afterward to deploy the plan.

🟡 New: an oversized brief now routes into a roadmap flow

| What | When you give plan a whole-app brief that spans several
|      | releases, plan now hands it to a roadmap step first: it
|      | proposes a sequenced set of milestones and asks you to
|      | confirm, merge, split, reorder, or reject the split before
|      | it plans any single milestone.
| When | Only when the brief reads as several milestones. A normal,
|      | single-release brief is unchanged — no roadmap step, no
|      | extra prompt.
| Note | This notice shows at most once per clone.

🟡 Optional: add project-specific implied surfaces

| What | You can add an optional overlay file at
|      | .milestone-config/implied-surfaces.md. The architect reads it
|      | alongside the plugin's bundled implied-surfaces reference when it
|      | breaks your brief into issues.
| Why  | The bundled reference is universal, so it can't carry capability
|      | clusters specific to your domain (a church app's "giving", say).
|      | Your overlay merges in additively — it can add a new capability
|      | and extend an existing one, but never removes a surface the
|      | bundled reference already defines.
| How  | Create .milestone-config/implied-surfaces.md and write one
|      | capability per ## heading with its implied surfaces beneath — the
|      | same shape as the bundled reference. Leave it out and the bundled
|      | reference is used as-is; an absent overlay is never an error.
| Note | This notice shows at most once per clone.

🟡 New: plan now adopts your repo's issue template

| What | plan now authors each issue to your repo's own issue template
|      | instead of its own built-in structure. It uses the template your
|      | driver config records as agentIssueTemplate; with no such key, the
|      | single template under .github/ISSUE_TEMPLATE/ (the reserved
|      | config.yml is not counted, so bug.yml + config.yml counts as one).
|      | No key and no single template keeps the built-in structure.
| When | Every plan run. The built-in structure authors four sections:
|      | Summary, Acceptance criteria, Design (recorded, consistent),
|      | and Dependencies, plus Non-goals when the issue records a scope
|      | boundary.
| Note | This notice shows once per clone, and again after an upgrade
|      | that revises it.

Step 0 — resolved:

| Input | Resolved |
|---|---|
| projectDocs | `project/` |
| Grounding digest | 1 slice: `project/conventions.md#Engineering conventions` |
| Implied-surfaces reference | bundled global; no project-local overlay → merged == global |
| Consumer issue-template | none (rung 3) → built-in §4 default |
| sourceGlobs | `["src/**"]` |
| uiSurfaceGlobs | `["src/pages/**", "src/components/**"]` |
| integrationBranch | `develop` |

Breakdown:

| Step | Outcome |
|---|---|
| 3 — architect (1 dispatch) | 7 candidates, 7 edges, 4 Waves, 1 product gap, multi-milestone signal `none` |
| 3.5 — pre-park | #B, #C parked on the shared gap before the fan-out (no author dispatched) |
| 3.5 — drop dependents | #D, #E, #F, #G dropped (transitively depend on a parked candidate) |
| 3.6 — front-door | signal `none` → no roadmap route |
| 4 — issue-author (1 dispatch) | #A AUTHORED, `[logic, risk:light]` |
| 5 — surviving set | #A only — Wave 1: #A |

🔴 Is this a versioned project? If so, what version is this milestone?

Nothing above it resolved: no `Milestone: <name> vX.Y.Z` line, no `versioning` key in feeder.json, no existing milestone titles, no `vX.Y.Z` git tags. Type a version and I'll use it verbatim as the milestone title; decline and the milestone takes the goal-derived name `Admin-to-member email messaging` with no version.

🔴 Confirm or override the milestone identity before you run `create`:

| Field | Value |
|---|---|
| Milestone title (exact) | UNRESOLVED — pending your answer to the prompt above |
| Version provenance | prompted (pending) |

🔴 1 of the 1 surviving issue is an implied companion surface, proposed for review — not named by your brief:

| Tag | Title | Marker |
|---|---|---|
| #A | Configure the admin-message sender in the shared email config | [implied — review / trim / augment] |

this is a starting set for YOUR app — what's missing?

Review, trim, or augment before you run `create`. The plan is deployable as-is.

🔴 1 product gap parked — the plan is partial:

| # | Item | Blocks |
|---|---|---|
| 1 | The suppression / unsubscribe (opt-out) policy for admin messages | #B, #C directly; #D, #E, #F, #G dropped as dependents |

Written:

| File | What |
|---|---|
| `.milestone-feeder/plan-admin-to-member-email-messaging.md` | the plan file (the build artifact) |
| `.milestone-feeder/needs-product-input-admin-to-member-email-messaging.md` | the parked product gap |

No GitHub state was written. Run `/milestone-feeder:create` to deploy the plan, or resolve the gap above and re-run `plan` for a fuller milestone.
```

### Runner run notes, verbatim

The runner's own account of the run, recorded exactly as returned.

# Run notes — `plan` preview, Admin-to-member email messaging

Procedure root: `/private/var/tmp/wsvc.pbm4cQC7/proc`. Consumer repo: `/private/var/tmp/wsvc.pbm4cQC7/repo`.
Steps run: 0, 1, 2, 3, 3.5, 3.6, 4, 5, 5.1, 7 plus the preview emit. Steps 3.7 (roadmap fan-out) skipped: `roadmapRouteTaken` false.

## Dispatches, in order

| # | Agent type | Subject |
|---|---|---|
| 1 | `milestone-feeder:architect` | Step 3 — the once-per-run breakdown of the whole brief into `CANDIDATES / EDGES / WAVES / PRODUCT_GAPS / SCOPE_SPANS_MULTIPLE_MILESTONES` |
| 2 | `milestone-feeder:issue-author` | Step 4 — author candidate `#A`, "Configure the admin-message sender in the shared email config" (the only candidate surviving Step 3.5) |

Total: 2. No re-dispatch fired (the Step-4 verify passed on the first attempt).

Both dispatches carried the two roots and the verbatim "your process working directory is NOT the consumer repo" instruction, every resolved Step-0 input pasted inline as content rather than as a path, and a mandatory PATHS READ return field.

## What each step produced

| Step | Outcome |
|---|---|
| 0 | `projectDocs: project/`; digest = 1 slice (`project/conventions.md#Engineering conventions`); implied-surfaces reference = bundled global, no overlay; issue-template = none (rung 3); shared keys `sourceGlobs ["src/**"]`, `uiSurfaceGlobs ["src/pages/**","src/components/**"]`, `integrationBranch develop`. Notices: §4, §5, §7 fire; §1 self-heal would fire; §2, §3 do not; §6 not evaluated (`plan` not in its Skills field). |
| 1 | Brief form = file path. Normalized to `{goal, in-scope, out-of-scope, surfaces}`. No `epicIssueNumber`, no `milestoneLine`. |
| 2 | `productGaps[]` empty; no STOP (the candidate set can be formed). |
| 3 | 7 candidates (#A–#G), 7 edges, 4 Waves, 1 `PRODUCT_GAPS` entry (suppression / unsubscribe policy, `blocks: [#B, #C]`), `SCOPE_SPANS_MULTIPLE_MILESTONES: none`. 4 candidates carry `disposition: implied` (#A, #E, #F, #G); none carries a `layer` (no layering convention in the docs → clause-9 no-op). |
| 3.5 | Pre-parked #B, #C (union of `blocks:`), no author dispatched for either. Drop-dependents took #D (deps #B, #C), #E (dep #B), #G (dep #B), then #F (deps #B, #E). Step-4 set reduced to {#A}. |
| 3.6 | `roadmapRouteTaken` initialized false; signal `none` → no route, fall through to Step 4 on the whole brief. |
| 4 | 1 dispatch, `STATUS: AUTHORED`, `LABELS: [logic, risk:light]`. Verify passed; no retry. |
| 5 | Surviving set = {#A}. Graph has no edges over that set. Waves re-rendered to `Wave 1: #A`. |
| 5.1 | Ladder ran to rung 4 and could not complete offline (see limitations). No version fabricated. |
| 7 | Plan file + needs-product-input report rendered. Multi-milestone advisory section omitted (signal `none`). Implied-surfaces anti-fixation prompt fired (one surviving implied candidate) with the verbatim string. |

## Preview limitations

1. **No `.milestone-config/feeder.json` on disk.** `repo/feeder-env.md` stands in for it per the harness. Step 0's "absent → invoke `milestone-feeder:setup`" branch was therefore NOT taken; the run treats the stated values as the resolved config.
2. **One-time notices were evaluated by inspection, not by executing the emitter twins.** Executing the recorded `bash` forms would write into the consumer repo (`.milestone-config/.gitignore` and `.milestone-config/.runtime/` markers), and the harness directs all output to `out/`. Every notice line in `out/preview-emit.md` is transcribed byte-for-byte from the emitters' recorded `printf` arguments, in file order. Consequence: no marker file exists, so a real re-run would print these notices again.
3. **§1 self-heal not performed.** The nested `.milestone-config/.gitignore` is absent, so the self-heal would fire and write it. It prints nothing, so it has no line in the preview emit. Not written, per limitation 2.
4. **Step 5.1 rung 3 is unresolvable here.** Sub-rung 1 needs a `gh api` milestones read: no GitHub remote and no network in this preview. Sub-rung 2 needs a `git tag` read: `/private/var/tmp/wsvc.pbm4cQC7/repo` is not a git repository (`git rev-parse` returns "not a git repository"). Both yield nothing, so the ladder falls to rung 4.
5. **Rung 4 (the prompt) could not be answered.** It is the procedure's one sanctioned interactive moment and this preview has no user turn. The prompt text is emitted verbatim in `out/preview-emit.md`; the plan file's `Milestone title (exact)` records UNRESOLVED with both rung-4 outcomes stated, and `Version provenance` records `prompted — PENDING`. No version string was invented to make the field complete.
6. **Scratch paths redirected.** The plan file and report would be written to `.milestone-feeder/plan-admin-to-member-email-messaging.md` and `.milestone-feeder/needs-product-input-admin-to-member-email-messaging.md`; they went to `out/plan.md` and `out/needs-product-input.md` instead. The real paths are recorded in the plan file and preview emit. The `.milestone-feeder/` self-ignore step was likewise not performed.
7. **Zero GitHub writes, and zero writes of any kind inside the consumer repo.** No `gh` call was made at all, mutating or reading.
8. **`build-roadmap` is not present in the procedure root.** It was never needed (signal `none`), so the Step-3.6 optional-skill degrade branch was not exercised.

## Judgment calls

1. **Step 2 recorded no product gap, deliberately.** The brief's core scope (compose a subject and body, pick recipients, send) is decidable, so there is no STOP. I did not pre-seed the suppression / opt-out question into the architect's brief, because doing so would pre-load its clause-8 sort. The architect received the full conventions slice including the undecided-policy note and raised the gap on its own.
2. **The architect's `blocks: [#B, #C]` was carried verbatim.** `plan` does not re-partition or second-guess the architect's structural read, and Step 3.5 takes the union of `blocks:` tags as given. Flagging it because the consequence is large: 5 of 7 candidates leave the milestone and the survivor is a single config issue. If a reviewer disagrees that recipient selection (#C) is blocked by the opt-out policy, that is the line to argue, and the procedure puts that argument in the plan review, not in an orchestrator override.
3. **Step-4 verify passed; one quality observation not treated as a failure.** #A's `ISSUE_BODY` carries no `## Dependencies` section. #A has no incoming dependency (the single edge touching it is `#B depends_on #A`, and #B is parked), so there is nothing to record there. The verify rule's malformed test is a missing/vacuous required field or a `STATUS` that is neither `AUTHORED` nor `PRODUCT_GAP` — none holds, so no retry fired. I did not hand-edit the returned body; the plan file carries it verbatim.
4. **Both agents were told the `src/**` files do not exist on disk.** The conventions doc names `src/services/EmailService.ts`, `src/config/email.ts`, `src/models/EmailDeliveryLog.ts`, `src/models/AuditLog.ts`, `src/components/AdminFormLayout.tsx`; none is present. Stating that enforces their rigor gates (cite what you verified) rather than relaxing them, and it routes citations to the one verifiable ref, `project/conventions.md#Engineering conventions`. #A's acceptance criteria still name `src/config/email.ts` as the file to change, which is the conventions doc's own directive rather than a claim about current disk state.
5. **Digest anchor form adapted to the real path.** Cited as `project/conventions.md#Engineering conventions`, not the procedure's illustrative `.project/<doc>.md#<section>`, because `projectDocs` is `project/` and no `.project/` exists.
6. **#A's file-map resolved empty**, per `docs/file-map.md` §2: no `path:line` or `path (anchor)` seed in its sketch or in the edge touching it (its grounding is a project-docs ref, which is neither seed form), and the keyword fallback matches no folder under `sourceGlobs: ["src/**"]`. Recorded as a degrade, not an error.
7. **One-line goal = "Admin-to-member email messaging"**, so the deterministic slug is `admin-to-member-email-messaging`.
8. **Waves re-rendered over the surviving set**, collapsing the architect's 4 Waves to `Wave 1: #A`, so no Wave line names a parked or dropped tag.
9. **`## Multi-milestone advisory` omitted entirely** from the plan file, per the field table's `none`-signal row.
10. **`Source brief:` recorded as the absolute path received** (`file:/private/var/tmp/wsvc.pbm4cQC7/repo/brief.md`).
11. **#F's dropped marker names both causes** (parked #B and dropped #E). The template's example shows a single cause; naming both is the truthful extension.
12. **The `[implied — review / trim / augment]` marker and the `this is a starting set for YOUR app — what's missing?` prompt string are byte-verbatim** from the contract, as are the three notice blocks.
