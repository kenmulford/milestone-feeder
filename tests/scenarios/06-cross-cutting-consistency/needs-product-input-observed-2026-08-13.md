🔴 Needs human input: Build the 10 admin entity list pages

These items blocked the milestone and were NOT guessed. Resolve each, then re-run plan.

| # | Item | Why blocked | Blocks |
|---|---|---|---|
| 1 | The standard column set for each of the 10 entities. | The standing docs hold one section, and it governs table behavior only (sort / filter, pagination, empty, loading, shared component); it names no column for any entity. `sourceGlobs` is `["src/**"]` and no `src/` exists in this repo, so there is no entity model and no sibling list page to derive the standard columns from. Brief line: "each page shows a table of its records with the standard columns for that entity". | No candidate pre-parked (`blocks: []`). Bears on every entity list page's column set. |
| 2 | Delete-action semantics: hard delete versus soft-delete plus restore, and whether a confirmation step is required. | `project/design-system.md#Design system: data tables (standing convention)` states no delete behavior, and `docs/implied-surfaces.md#new-entity-baseline` offers both forms ("or soft-delete + restore where the data warrants it") without a default. The choice differs by entity between financial records (Orders, Invoices, Refunds, Shipments) and catalog rows. Brief line: "each row carries row actions: view / edit / delete". | No candidate pre-parked (`blocks: []`). Bears on the delete row action on every entity list page. |
| 3 | Whether Audit Log records may be edited or deleted at all. | The brief applies view / edit / delete to every entity, including the audit trail, while `docs/implied-surfaces.md#new-entity-baseline` treats audit as the record of "who created or changed a record, and when". Neither the brief nor `project/design-system.md#Design system: data tables (standing convention)` states whether audit records are mutable, and the answer is a compliance decision, not a convention. Brief lines: the Audit Log entry in the page list, with "each row carries row actions: view / edit / delete". | #J (Build the Audit Log admin list page), pre-parked before authoring. |
| 4 | Which admin roles may view each entity's list and invoke each row action, enforced server-side. | The standing docs state no roles or authorization convention and the repo carries no source to mirror. `docs/implied-surfaces.md#new-entity-baseline` (permissions) and `docs/implied-surfaces.md#cross-cutting` ("results scoped to what the viewer is allowed to see") name the surface, but the role matrix has no conventional default, and each page's unauthorized state depends on it. Brief line: "Build the admin app's list pages, one page per admin entity". | No candidate pre-parked (`blocks: []`). Bears on every list page's unauthorized state and on each row action's server-side check. |
